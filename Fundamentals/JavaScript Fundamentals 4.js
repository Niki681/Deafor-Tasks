//4. Write a program that takes the user for a string, then displays the string in reverse order.

let str = "";
function wordRevOrd(str){
 for (let i = str.length - 1; i >= 0; i--) {
    console.log(str[i]);
    
 }
}
wordRevOrd("NIki")


