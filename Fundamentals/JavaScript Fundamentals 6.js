//6. Write a program that takes the user to enter three numbers, then finds and displays 
//the largest of the three numbers.


function largestNumber(a, b, c) {
    let max = Math.max(a, b, c);
    console.log(max);
  }
  
  largestNumber(10, 15, 20);