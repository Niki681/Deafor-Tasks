//8. Write a program that takes the user to enter a 
//word and then displays the word with the first letter capitalized.

function word (params) {
    let resutlt = params[0].toUpperCase(params)+ params.substring(1);
    console.log(resutlt);
}

word("luckyGuy")



